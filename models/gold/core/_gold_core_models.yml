version: 2

models:
  - name: dim_locations
    config:
      materialized: table
    columns:
      - name: location_id
        data_tests:
          - unique
          - not_null
  - name: fact_iot_events
    config:
      materialized: table
  - name: dim_date
    config:
      materialized: table
    columns:
      - name: date
        data_tests:
          - unique
          - not_null
