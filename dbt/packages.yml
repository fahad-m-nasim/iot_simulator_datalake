packages:
  # dbt-utils: Common SQL utilities
  - package: dbt-labs/dbt_utils
    version: ">=1.1.0"
  
  # dbt-expectations: Data quality testing (Great Expectations style)
  - package: calogica/dbt_expectations
    version: ">=0.10.0"
  
  # dbt-date: Date/time utilities
  - package: calogica/dbt_date
    version: ">=0.10.0"
  
  # Audit helper: Compare model results
  - package: dbt-labs/audit_helper
    version: ">=0.9.0"
  
  # Codegen: Generate YAML and model code
  - package: dbt-labs/codegen
    version: ">=0.12.0"
